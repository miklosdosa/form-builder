import { render } from "@testing-library/react";
import { FieldGroup } from "./FieldGroup";

describe("FieldGroup", () => {
  it("should render properly", () => {
    render(<FieldGroup />);
  });
});
