import { render } from "@testing-library/react";
import { RadioField } from "./RadioField";

describe("RadioField", () => {
  it("should render properly", () => {
    render(<RadioField />);
  });
});
